// --------------------------------------------------------
// app/state.js
// Manages application level state.
// --------------------------------------------------------
(function(APP) {

	// ECMAScript 5 Strict Mode
	"use strict";

	// Module state.
	var state = APP.state = {
		// Current module.
		module: undefined,

		// List of modules.
		moduleList: []
	};

}(this.APP));
